{
  "rules": {
    ".read": false,
    ".write": false,

    "meta": {
      ".read": "auth != null && root.child('users').child(auth.uid).exists()",
      ".write": "auth != null && root.child('users').child(auth.uid).child('role').val() === 'masterAdmin'"
    },

    "users": {
      ".read": "auth != null && root.child('users').child(auth.uid).exists()",
      "$uid": {
        ".write": "auth != null && ( ( auth.uid === $uid && ( !data.exists() || newData.child('role').val() !== 'masterAdmin' || data.child('role').val() === 'masterAdmin' ) ) || root.child('users').child(auth.uid).child('role').val() === 'masterAdmin' )"
      }
    },

    "teams": {
      ".read": "auth != null && root.child('users').child(auth.uid).exists()",
      "$teamId": {
        ".write": "auth != null && ( root.child('users').child(auth.uid).child('role').val() === 'masterAdmin' || root.child('teams').child($teamId).child('leadUids').child(auth.uid).val() === true )",
        "leadUids": {
          "$uid": {
            ".write": "auth != null && auth.uid === $uid && root.child('users').child(auth.uid).exists()"
          }
        },
        "memberUids": {
          "$uid": {
            ".write": "auth != null && auth.uid === $uid && root.child('users').child(auth.uid).exists()"
          }
        }
      }
    },

    "projects": {
      ".read": "auth != null && root.child('users').child(auth.uid).exists()",
      "$projectId": {
        ".write": "auth != null && ( root.child('users').child(auth.uid).child('role').val() === 'masterAdmin' || root.child('teams').child(data.child('teamId').val()).child('leadUids').child(auth.uid).val() === true || root.child('teams').child(newData.child('teamId').val()).child('leadUids').child(auth.uid).val() === true )",
        "stages": {
          "$stageId": {
            ".write": "auth != null && root.child('users').child(auth.uid).exists()"
          }
        }
      }
    },

    "pendingInvites": {
      "$emailKey": {
        ".read": "auth != null && root.child('users').child(auth.uid).child('role').val() === 'masterAdmin'",
        ".write": "auth != null && ( root.child('users').child(auth.uid).child('role').val() === 'masterAdmin' || root.child('teams').child(data.child('teamId').val()).child('leadUids').child(auth.uid).val() === true || root.child('teams').child(newData.child('teamId').val()).child('leadUids').child(auth.uid).val() === true || data.child('email').val() === auth.token.email || (!data.exists() && !newData.exists()) )"
      }
    },

    "userProjectIndex": {
      "$emailKey": {
        ".read": "auth != null && root.child('users').child(auth.uid).exists()",
        ".write": "auth != null && root.child('users').child(auth.uid).exists()"
      }
    }
  }
}
